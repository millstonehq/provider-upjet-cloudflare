apiVersion: cloudflare.millstone.tech/v1beta1
kind: ProviderConfig
metadata:
  name: {{ include "provider-cloudflare.providerConfigName" . }}
  labels:
    {{- include "provider-cloudflare.labels" . | nindent 4 }}
  annotations:
    {{- include "provider-cloudflare.annotations" (dict "root" . "syncWave" .Values.syncWave.providerConfig) | nindent 4 }}
spec:
  credentials:
    source: Secret
    secretRef:
      name: {{ .Values.providerConfig.secretRef.name }}
      namespace: {{ .Values.providerConfig.secretRef.namespace }}
      key: {{ .Values.providerConfig.secretRef.key }}
